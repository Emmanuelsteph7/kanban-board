{
  "name": "kanban-board",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "server:dev": "npm run dev --workspace=server"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
